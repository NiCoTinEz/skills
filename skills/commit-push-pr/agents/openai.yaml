interface:
  display_name: "Commit Push PR"
  short_description: "Commit on this branch, push and open a PR"
  default_prompt: "Use $commit-push-pr to commit on the current branch, push it and open a pull request."
